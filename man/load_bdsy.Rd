% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdsy2025.R
\name{load_bdsy}
\alias{load_bdsy}
\title{A guide to load_bdsy()}
\usage{
load_bdsy(cohort, install_missing = TRUE)
}
\arguments{
\item{install_missing}{Logical. If \code{TRUE}, missing packages will be installed automatically. If \code{FALSE}, the function will only attempt to load already-installed packages.}

\item{sds_class}{A string indicating the cohort (e.g., \code{"ci"}). Must be one of the defined cohort names in \code{cohort_packages}.}
}
\description{
\itemize{
\item This function automatically loads and optionally installs R packages for the causal inference research group
\item Designed to streamline course preparation by handling core packages with one call.
}
}
\details{
\itemize{
\item \strong{Valid cohort names}: \code{"ci"}
\item The function stops with an error and notifies user if:
\itemize{
\item The \code{cohort} argument is unquoted (e.g., \code{load_bdsy(ci)} instead of \code{load_bdsy("ci")}).
\item The cohort name is not one of the included cohorts in \code{cohort_packages}.
}
}
}
\examples{
\dontrun{
# Standard usage (installs missing packages by default):
load_bdsy("ci")

# Loads without installing missing packages:
load_bdsy("ci", install_missing = FALSE)
}

}
